Save your scripts in the 'work' folder.

The "work" folder can be put anywhere in the file system.

Path to folder must be specified in SOFE.getWorkPath()

Path setting to access main files in "work/mystuff" is done by ">> SOFE.open('mystuff')"